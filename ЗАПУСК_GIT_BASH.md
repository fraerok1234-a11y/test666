# 🚀 ЗАПУСК В GIT BASH (для Windows)

## ⚡ ПРАВИЛЬНЫЙ СПОСОБ:

### 1. Откройте Git Bash (не обычную командную строку!)

### 2. Перейдите в папку проекта:
```bash
cd /e/sait_printer/TestApp
```

### 3. Запустите приложение:
```bash
npm run web
```

### 4. Откройте браузер и перейдите по адресу:
```
http://localhost:19006
```

## 🔧 ЕСЛИ НЕ РАБОТАЕТ:

### Проблема: "Missing script: web"
**Решение:** Вы не в той папке! Убедитесь, что:
1. Вы в папке `/e/sait_printer/TestApp`
2. В папке есть файл `package.json`

### Проблема: "Cannot find path"
**Решение:** Используйте правильный путь:
```bash
cd /e/sait_printer/TestApp
```
(не `E:\sait_printer\TestApp`)

### Проблема: "npm not found"
**Решение:** Установите Node.js с https://nodejs.org/

## 📱 ДЛЯ ТЕЛЕФОНА:

### 1. Установите Expo Go:
- **iOS**: App Store → "Expo Go"
- **Android**: Google Play → "Expo Go"

### 2. В Git Bash запустите:
```bash
cd /e/sait_printer/TestApp
npx expo start
```

### 3. Отсканируйте QR-код

## ✅ ПРОВЕРКА:

Если все правильно, вы увидите:
- Сообщение "Starting Metro bundler"
- Локальный адрес (например, http://localhost:19006)
- QR-код для телефона

---

**Важно: Используйте Git Bash, а не обычную командную строку Windows!** 🎯

