# 🎨 Как форматировать приложение

## Где находятся стили

### 1. Основные стили приложения
**Файл:** `App.tsx` (строки 244-360)

Здесь находятся стили для:
- Заголовков (`title`, `subtitle`)
- Кнопок (`restartButton`, `menuButton`)
- Вопросов (`questionText`, `optionButton`)
- Прогресс-бара (`progressBar`, `progressFill`)

### 2. Стили карточек тестов
**Файл:** `components/TestCard.tsx` (строки 32-62)

Здесь стили для карточек в главном меню:
- Размер иконок
- Цвета текста
- Отступы и скругления

### 3. Цветовая палитра
**Файл:** `theme/colors.ts`

Все цвета приложения в одном месте:
- Цвета тестов (quiz, personality, assessment)
- Цвета для результатов (success, warning, error)
- Цвета фона и текста

### 4. Отступы и размеры
**Файл:** `theme/spacing.ts`

Стандартные отступы и размеры:
- Размеры шрифтов
- Отступы между элементами
- Радиусы скругления

---

## Как изменить дизайн

### Изменить цвета карточек тестов

Открой `constants/tests.ts` и измени цвета:

```typescript
export const TEST_CONFIG = {
	[TEST_TYPES.QUIZ]: {
		title: "📚 Викторина",
		subtitle: "Проверьте свои знания",
		color: "#3498db",  // ← Измени здесь (синий)
		questions: QUIZ_QUESTIONS
	},
	[TEST_TYPES.PERSONALITY]: {
		title: "🧠 Личностный тест",
		subtitle: "Узнайте больше о себе",
		color: "#e74c3c",  // ← Измени здесь (красный)
		questions: PERSONALITY_QUESTIONS
	},
	[TEST_TYPES.ASSESSMENT]: {
		title: "💼 Оценка навыков",
		subtitle: "Оцените свой уровень",
		color: "#27ae60",  // ← Измени здесь (зеленый)
		questions: ASSESSMENT_QUESTIONS
	}
};
```

### Изменить размер шрифта заголовка

В `App.tsx` найди стиль `title` (строка 267):

```typescript
title: {
	fontSize: 28,  // ← Измени размер здесь
	fontWeight: 'bold',
	textAlign: 'center',
	marginBottom: 10,
	color: '#2c3e50',
},
```

### Изменить цвет фона

В `App.tsx` найди стиль `container` (строка 252):

```typescript
container: {
	flex: 1,
	padding: 20,
	backgroundColor: '#f8f9fa',  // ← Измени цвет здесь
},
```

### Изменить размер карточек

В `components/TestCard.tsx` найди стиль `card` (строка 33):

```typescript
card: {
	padding: 20,        // ← Внутренние отступы
	borderRadius: 12,   // ← Скругление углов
	marginBottom: 15,   // ← Отступ снизу
	minHeight: 100,     // ← Минимальная высота
	// ...
},
```

### Изменить цвет прогресс-бара

В `App.tsx` найди стиль `progressFill` (строка 295):

```typescript
progressFill: {
	height: '100%',
	backgroundColor: '#27ae60',  // ← Измени цвет здесь
},
```

---

## Примеры изменений

### Темная тема

Измени в `App.tsx`:
```typescript
container: {
	backgroundColor: '#1a1a1a',  // Темный фон
},
title: {
	color: '#ffffff',  // Белый текст
},
```

### Увеличить размер карточек

В `components/TestCard.tsx`:
```typescript
card: {
	minHeight: 150,  // Было 100
	padding: 30,     // Было 20
},
```

### Изменить шрифты

В `App.tsx` добавь `fontFamily`:
```typescript
title: {
	fontSize: 28,
	fontWeight: 'bold',
	fontFamily: 'Georgia',  // ← Добавь здесь
	// ...
},
```

---

## Быстрые изменения

### Цвета тестов
- Викторина: `#3498db` (синий)
- Личностный: `#e74c3c` (красный)
- Оценка: `#27ae60` (зеленый)

### Основные цвета
- Фон: `#f8f9fa` (светло-серый)
- Текст: `#2c3e50` (темно-серый)
- Вторичный текст: `#7f8c8d` (серый)

### Размеры
- Заголовок: 28px
- Подзаголовок: 16px
- Текст вопроса: 20px
- Варианты ответов: 16px

---

## Советы

1. **Используй файл `theme/colors.ts`** для централизованного управления цветами
2. **Используй `theme/spacing.ts`** для единообразных отступов
3. **Тестируй изменения** - после каждого изменения обнови страницу (F5)
4. **Сохраняй копии** - перед большими изменениями сделай бэкап

---

## Где что менять

| Что изменить | Файл | Строка |
|-------------|------|--------|
| Цвета карточек | `constants/tests.ts` | 164-183 |
| Стили главного меню | `App.tsx` | 244-360 |
| Стили карточек | `components/TestCard.tsx` | 32-62 |
| Цветовая палитра | `theme/colors.ts` | Все |
| Отступы | `theme/spacing.ts` | Все |
| Стили результатов | `components/ResultsCard.tsx` | 72-145 |




