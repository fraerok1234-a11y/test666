# 📝 Как форматировать текст в приложении

## Где находятся тексты

### 1. Заголовок и подзаголовок главного меню
**Файл:** `App.tsx` (строки 114-117)

```typescript
const renderMainMenu = () => (
	<View style={styles.container}>
		<Text style={styles.title}>Тестовое Приложение</Text>  ← Заголовок
		<Text style={styles.subtitle}>Проверьте свои знания</Text>  ← Подзаголовок
```

**Как изменить:**
- Замени `"Тестовое Приложение"` на свой заголовок
- Замени `"Проверьте свои знания"` на свой подзаголовок

---

### 2. Название и описание викторины
**Файл:** `constants/tests.ts` (строки 164-170)

```typescript
export const TEST_CONFIG = {
	[TEST_TYPES.QUIZ]: {
		title: "📚 Викторина",  ← Название на карточке
		subtitle: "Проверьте свои знания",  ← Описание на карточке
		color: "#3498db",
		questions: QUIZ_QUESTIONS
	},
```

**Как изменить:**
- Замени `"📚 Викторина"` на свое название
- Замени `"Проверьте свои знания"` на свое описание
- Можно убрать эмодзи или добавить другой

---

### 3. Вопросы викторины
**Файл:** `constants/tests.ts` (строки 16-66)

```typescript
export const QUIZ_QUESTIONS: Question[] = [
	{
		id: 1,
		question: "Столица Украины?",  ← Текст вопроса
		options: ["Львов", "Харьков", "Киев", "Одесса"],  ← Варианты ответов
		correctAnswer: 2,  ← Правильный ответ (индекс: 0, 1, 2 или 3)
		category: "География"  ← Категория вопроса
	},
	// ... остальные вопросы
];
```

**Как изменить:**
- Замени `question` на свой вопрос
- Замени `options` на свои варианты ответов
- Укажи `correctAnswer` (0 = первый вариант, 1 = второй, и т.д.)
- Замени `category` на свою категорию

**Пример:**
```typescript
{
	id: 1,
	question: "Какой язык программирования самый популярный?",
	options: ["Python", "JavaScript", "Java", "C++"],
	correctAnswer: 1,  // JavaScript (второй вариант)
	category: "Программирование"
},
```

---

### 4. Тексты кнопок
**Файл:** `App.tsx`

**Кнопка "Назад":** (строка 185)
```typescript
<Text style={styles.backButtonText}>← Назад</Text>
```

**Кнопка "Пройти тест заново":** (строка 200)
```typescript
<Text style={styles.buttonText}>Пройти тест заново</Text>
```

**Кнопка "Главное меню":** (строка 204)
```typescript
<Text style={styles.menuButtonText}>Главное меню</Text>
```

---

### 5. Тексты результатов
**Файл:** `utils/testUtils.ts` (строки 52-60)

```typescript
const getQuizFeedback = (percentage: number): string => {
	if (percentage >= 90) return 'Превосходно! Вы настоящий эксперт! 🌟🏆';
	if (percentage >= 80) return 'Отлично! Вы показали отличные знания! 🌟';
	if (percentage >= 70) return 'Очень хорошо! Вы хорошо подготовлены! 👍';
	if (percentage >= 60) return 'Хорошо! Есть куда расти. 📈';
	if (percentage >= 50) return 'Удовлетворительно. Нужно подтянуть знания. 📚';
	if (percentage >= 40) return 'Попробуйте еще раз. Учитесь и развивайтесь! 💪';
	return 'Не отчаивайтесь! Каждый эксперт когда-то был новичком! 🌱';
};
```

**Как изменить:**
- Замени тексты на свои
- Измени проценты (>= 90, >= 80, и т.д.)
- Добавь или убери эмодзи

---

### 6. Тексты экрана оплаты (Paywall)
**Файл:** `components/Paywall.tsx`

**Заголовок:** (строка ~50)
```typescript
<Text style={styles.title}>Полный доступ к тестам</Text>
```

**Описание:** (строка ~52)
```typescript
<Text style={styles.subtitle}>
	Приобретите полный доступ ко всем тестам и функциям приложения
</Text>
```

**Список преимуществ:** (строки ~55-70)
```typescript
<Text style={styles.featureText}>Все типы тестов</Text>
<Text style={styles.featureText}>Неограниченное количество прохождений</Text>
<Text style={styles.featureText}>Детальные результаты</Text>
<Text style={styles.featureText}>Без рекламы</Text>
```

**Кнопка покупки:** (строка ~100)
```typescript
<Text style={styles.purchaseButtonText}>
	Купить полный доступ
</Text>
```

---

## Примеры форматирования

### Добавить перенос строки
В React Native используй несколько `<Text>` компонентов:
```typescript
<Text style={styles.title}>
	Тестовое{'\n'}Приложение
</Text>
```

### Жирный текст
```typescript
<Text style={styles.title}>
	<Text style={{fontWeight: 'bold'}}>Жирный</Text> обычный текст
</Text>
```

### Курсив
```typescript
<Text style={{fontStyle: 'italic'}}>Курсивный текст</Text>
```

### Разные цвета в одном тексте
```typescript
<Text>
	<Text style={{color: '#3498db'}}>Синий</Text>
	<Text style={{color: '#e74c3c'}}>Красный</Text>
</Text>
```

---

## Быстрые изменения

### Изменить название приложения
1. Открой `App.tsx`
2. Найди строку 116: `"Тестовое Приложение"`
3. Замени на свое название

### Изменить описание
1. Открой `App.tsx`
2. Найди строку 117: `"Проверьте свои знания"`
3. Замени на свое описание

### Добавить новый вопрос
1. Открой `constants/tests.ts`
2. Найди массив `QUIZ_QUESTIONS`
3. Добавь новый объект:
```typescript
{
	id: 8,  // Следующий номер
	question: "Твой вопрос?",
	options: ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
	correctAnswer: 0,  // Индекс правильного ответа
	category: "Твоя категория"
},
```

### Изменить текст кнопки
1. Открой `App.tsx`
2. Найди нужную кнопку (строки 185, 200, 204)
3. Замени текст в кавычках

---

## Где что находится

| Текст | Файл | Строка |
|-------|------|--------|
| Заголовок приложения | `App.tsx` | 116 |
| Подзаголовок | `App.tsx` | 117 |
| Название викторины | `constants/tests.ts` | 166 |
| Описание викторины | `constants/tests.ts` | 167 |
| Вопросы | `constants/tests.ts` | 16-66 |
| Тексты результатов | `utils/testUtils.ts` | 52-60 |
| Кнопка "Назад" | `App.tsx` | 185 |
| Кнопка "Заново" | `App.tsx` | 200 |
| Кнопка "Меню" | `App.tsx` | 204 |
| Тексты оплаты | `components/Paywall.tsx` | ~50-100 |

---

## Советы

1. **Сохраняй копии** - перед большими изменениями сделай бэкап
2. **Проверяй синтаксис** - после изменений обнови страницу (F5)
3. **Используй кавычки** - все тексты должны быть в кавычках `"текст"`
4. **Правильные индексы** - `correctAnswer` начинается с 0 (0, 1, 2, 3)
5. **Эмодзи** - можно использовать любые эмодзи в текстах

---

## Частые вопросы

**Как добавить больше вопросов?**
→ Добавь новые объекты в массив `QUIZ_QUESTIONS` в `constants/tests.ts`

**Как изменить количество вариантов ответов?**
→ Измени массив `options` (можно 2, 3, 4, 5 вариантов)

**Как убрать категорию вопроса?**
→ Удали строку `category: "География"` или оставь пустым

**Как изменить текст "Вопрос X из Y"?**
→ В `App.tsx` строка 154: `Вопрос {currentQuestion + 1} из {questions.length}`




