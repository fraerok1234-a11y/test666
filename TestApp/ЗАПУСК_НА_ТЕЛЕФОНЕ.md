# 📱 Как запустить на телефоне

## Вариант 1: Через Expo Go (для быстрого тестирования)

### Шаг 1: Установи Expo Go на телефон
- **Android**: Открой Google Play Store → найди "Expo Go" → установи
- **iOS**: Открой App Store → найди "Expo Go" → установи

### Шаг 2: Запусти сервер на компьютере

Открой Git Bash и выполни:
```bash
cd /e/sait_printer/TestApp
npx expo start
```

### Шаг 3: Подключи телефон

**Для Android:**
1. Убедись, что телефон и компьютер в одной Wi-Fi сети
2. В терминале появится QR-код
3. Открой Expo Go на телефоне
4. Нажми "Scan QR code"
5. Отсканируй QR-код из терминала

**Для iOS:**
1. Убедись, что телефон и компьютер в одной Wi-Fi сети
2. Открой приложение "Камера" на iPhone
3. Наведи на QR-код в терминале
4. Нажми на уведомление → откроется в Expo Go

### ⚠️ Важно:
- Покупки через Expo Go **не работают** (только тесты)
- Для покупок нужна собранная версия (см. Вариант 2)

---

## Вариант 2: Собрать APK для установки (с покупками)

### Шаг 1: Установи зависимости
```bash
cd /e/sait_printer/TestApp
npm install --legacy-peer-deps
```

### Шаг 2: Войди в Expo
```bash
npx expo login
```
(Создай аккаунт на https://expo.dev если нет)

### Шаг 3: Собери APK
```bash
npx eas build --platform android --profile preview
```

Это создаст APK файл, который можно установить на телефон.

### Шаг 4: Установи APK на телефон
1. Дождись завершения сборки (10-20 минут)
2. Скачай APK из Expo Dashboard
3. Перешли APK на телефон
4. Включи "Установка из неизвестных источников" в настройках Android
5. Установи APK

---

## Вариант 3: Быстрый тест через USB (Android)

### Требования:
- Android Studio установлен
- USB Debugging включен на телефоне
- Телефон подключен по USB

### Команда:
```bash
cd /e/sait_printer/TestApp
npm run android
```

Приложение установится и запустится на телефоне автоматически.

---

## Решение проблем

### "Не могу найти QR-код"
- Убедись, что `npx expo start` запущен
- Проверь, что телефон и компьютер в одной Wi-Fi сети

### "Не могу подключиться"
- Проверь файрволл Windows
- Попробуй ввести адрес вручную в Expo Go: `exp://192.168.x.x:8081`

### "Покупки не работают"
- Покупки работают только в собранной версии (APK/AAB)
- В Expo Go покупки недоступны

---

## Рекомендации

- **Для тестирования тестов** → используй Expo Go (Вариант 1)
- **Для тестирования покупок** → собери APK (Вариант 2)
- **Для публикации в Play Store** → собери AAB через `npx eas build --platform android --profile production`




