# 📤 Как залить программу на GitHub

## 🎯 Быстрый способ (через веб-интерфейс)

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте [GitHub.com](https://github.com) и войдите
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `test-app` (или другое имя)
   - **Description**: "Тестове додаток для перевірки знань з вибором тем"
   - Выберите **Public** или **Private**
   - **НЕ** отмечайте "Add a README file"
4. Нажмите **"Create repository"**

### Шаг 2: Загрузите файлы через веб

1. В новом репозитории нажмите **"uploading an existing file"**
2. Перетащите **все файлы из папки `TestApp`** (кроме `node_modules`)
3. Нажмите **"Commit changes"**

✅ **Готово!** Ваш код на GitHub.

---

## 💻 Способ через командную строку (Git Bash)

### Шаг 1: Подготовка

Откройте **Git Bash** в папке `e:\sait_printer` и выполните:

```bash
# Перейдите в корневую директорию
cd /e/sait_printer

# Проверьте статус
git status
```

### Шаг 2: Добавление файлов

```bash
# Добавьте папку TestApp
git add TestApp/

# Проверьте, что будет добавлено
git status
```

### Шаг 3: Создание коммита

```bash
# Создайте коммит
git commit -m "Додано тестове додаток з вибором тем та категорій"
```

### Шаг 4: Создание репозитория на GitHub

1. Откройте [GitHub.com](https://github.com)
2. **"+"** → **"New repository"**
3. Название: `test-app`
4. **НЕ** отмечайте "Initialize with README"
5. Нажмите **"Create repository"**

### Шаг 5: Подключение и загрузка

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/test-app.git

# Или если remote уже существует, обновите URL
git remote set-url origin https://github.com/YOUR_USERNAME/test-app.git

# Проверьте подключение
git remote -v

# Загрузите код на GitHub
git push -u origin master

# Или если ваша ветка называется main:
git push -u origin main
```

### Если нужна авторизация:

GitHub больше не принимает пароли. Используйте **Personal Access Token**:

1. GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. **Generate new token (classic)**
3. Отметьте `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен вместо пароля

---

## 🌐 Размещение на GitHub Pages (чтобы приложение работало онлайн)

### После загрузки кода:

1. В репозитории перейдите в **Settings**
2. Прокрутите до **Pages**
3. В разделе **Source**:
   - Branch: `master` или `main`
   - Folder: `/` (root)
4. Нажмите **Save**

Через 2-3 минуты приложение будет доступно по адресу:
**`https://YOUR_USERNAME.github.io/test-app`**

---

## 📱 Альтернатива: GitHub Desktop

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Откройте GitHub Desktop
3. **File** → **Add Local Repository**
4. Выберите `e:\sait_printer`
5. Нажмите **Publish repository**
6. Заполните название и описание
7. Нажмите **Publish Repository**

---

## ⚠️ Важно!

- **НЕ** загружайте файлы с паролями
- `node_modules/` уже в `.gitignore` (не загрузится)
- Файл `questions.docx` будет загружен (это нормально)

---

## 🔧 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Обновить код с GitHub
git pull origin master

# Создать новую ветку
git checkout -b feature/new-feature
```

---

## ❓ Если возникли проблемы

1. **Ошибка авторизации**: Используйте Personal Access Token
2. **Конфликты**: `git pull origin master` перед `git push`
3. **Большой файл**: GitHub ограничивает до 100MB на файл


