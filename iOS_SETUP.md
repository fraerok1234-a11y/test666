# 🍎 Настройка для iOS

## Требования для iOS разработки

### Обязательные требования:
- **macOS** (обязательно для iOS разработки)
- **Xcode** (последняя версия)
- **Node.js** (версия 18 или выше)
- **npm** или **yarn**

### Рекомендуемые требования:
- **iOS Simulator** (входит в Xcode)
- **Физическое iOS устройство** для тестирования
- **Apple Developer Account** (для публикации в App Store)

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
cd TestApp
npm install
```

### 2. Запуск на iOS симуляторе
```bash
npm run ios
```

### 3. Запуск через Expo Go (для тестирования на устройстве)
```bash
npx expo start
```
Затем отсканируйте QR-код в приложении Expo Go на вашем iOS устройстве.

## 📱 Запуск на физическом устройстве

### Через Expo Go (самый простой способ):
1. Установите приложение **Expo Go** из App Store
2. Запустите `npx expo start` в терминале
3. Отсканируйте QR-код камерой или в приложении Expo Go

### Через Xcode (для полной разработки):
1. Установите Xcode из Mac App Store
2. Запустите `npx expo run:ios`
3. Выберите симулятор или подключенное устройство

## 🔧 Настройка Xcode

### Установка Xcode:
1. Откройте **Mac App Store**
2. Найдите **Xcode**
3. Установите последнюю версию
4. Запустите Xcode и примите лицензионное соглашение

### Настройка командной строки:
```bash
sudo xcode-select --install
```

### Проверка установки:
```bash
xcode-select --print-path
```

## 📦 Сборка для App Store

### Через Expo Application Services (EAS):
```bash
# Установка EAS CLI
npm install -g @expo/eas-cli

# Вход в аккаунт Expo
eas login

# Настройка проекта
eas build:configure

# Сборка для iOS
eas build --platform ios
```

### Локальная сборка:
```bash
# Установка зависимостей для iOS
npx expo install

# Сборка проекта
npx expo run:ios --configuration Release
```

## 🎯 Настройка для разработки

### Конфигурация app.json:
```json
{
  "expo": {
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.yourcompany.testapp",
      "buildNumber": "1.0.0",
      "infoPlist": {
        "CFBundleDisplayName": "Тестовое Приложение",
        "UIStatusBarStyle": "dark-content"
      }
    }
  }
}
```

### Настройка подписи:
1. Откройте проект в Xcode
2. Выберите проект в навигаторе
3. Перейдите в **Signing & Capabilities**
4. Настройте **Team** и **Bundle Identifier**

## 🐛 Отладка

### Логи в консоли:
```bash
npx expo start --clear
```

### Отладка в Xcode:
1. Запустите проект через `npx expo run:ios`
2. Откройте Xcode
3. Используйте **Debug Navigator** для отслеживания производительности

### React Native Debugger:
```bash
# Установка
brew install --cask react-native-debugger

# Запуск
open "rndebugger://set-debugger-loc?host=localhost&port=19000"
```

## 📊 Тестирование

### Unit тесты:
```bash
npm test
```

### E2E тесты (с Detox):
```bash
# Установка Detox
npm install -g detox-cli

# Настройка
detox init

# Запуск тестов
detox test
```

## 🚀 Развертывание

### TestFlight:
1. Создайте сборку через EAS
2. Загрузите в App Store Connect
3. Настройте TestFlight для тестирования

### App Store:
1. Подготовьте метаданные приложения
2. Создайте релизную сборку
3. Загрузите в App Store Connect
4. Отправьте на проверку

## 🔒 Безопасность

### Рекомендации:
- Используйте HTTPS для всех сетевых запросов
- Не храните чувствительные данные в коде
- Используйте Keychain для хранения паролей
- Включите App Transport Security

### Настройка ATS:
```json
{
  "expo": {
    "ios": {
      "infoPlist": {
        "NSAppTransportSecurity": {
          "NSAllowsArbitraryLoads": false
        }
      }
    }
  }
}
```

## 📱 Поддерживаемые устройства

### iOS версии:
- iOS 13.0 и выше
- iPhone 6s и новее
- iPad Air 2 и новее

### Разрешения экранов:
- iPhone: 375x667 - 428x926
- iPad: 768x1024 - 1024x1366

## 🎨 Адаптация под iOS

### Human Interface Guidelines:
- Используйте системные шрифты
- Следуйте принципам iOS дизайна
- Адаптируйте под разные размеры экранов
- Используйте Safe Area

### Настройка Safe Area:
```typescript
import { SafeAreaView } from 'react-native';

<SafeAreaView style={styles.container}>
  {/* Ваш контент */}
</SafeAreaView>
```

## 📞 Поддержка

### Полезные ресурсы:
- [React Native iOS Guide](https://reactnative.dev/docs/running-on-device)
- [Expo iOS Guide](https://docs.expo.dev/workflow/ios-simulator/)
- [Apple Developer Documentation](https://developer.apple.com/ios/)

### Сообщество:
- [React Native Community](https://github.com/react-native-community)
- [Expo Discord](https://discord.gg/expo)

---

**Удачной разработки! 🚀**

