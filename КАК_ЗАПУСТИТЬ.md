# 🚀 Как запустить приложение

## Вариант 1: Запуск в браузере (самый простой способ)

### Шаг 1: Установите зависимости
```bash
cd TestApp
npm install
```

### Шаг 2: Запустите веб-версию
```bash
npm run web
```

Откроется браузер с приложением. **Примечание:** Покупки в веб-версии работать не будут, но можно протестировать интерфейс и тесты.

---

## Вариант 2: Запуск на Android устройстве/эмуляторе

### Требования:
1. **Node.js** (версия 18+) - [скачать](https://nodejs.org/)
2. **Android Studio** - [скачать](https://developer.android.com/studio)
3. **Expo Go** (для тестирования) или собранное приложение

### Шаг 1: Установите зависимости
```bash
cd TestApp
npm install
```

### Шаг 2: Настройте Android

#### Вариант A: Использовать Expo Go (проще)

1. Установите **Expo Go** на Android устройство из Play Store
2. Запустите сервер разработки:
```bash
npm start
```
3. Отсканируйте QR-код в Expo Go приложении

**Ограничение:** Покупки через Expo Go работать не будут, только тесты.

#### Вариант B: Собрать нативное приложение (для покупок)

1. Установите Android Studio и настройте эмулятор
2. Войдите в Expo:
```bash
npx expo login
```
3. Запустите на Android:
```bash
npm run android
```

Это соберет и запустит приложение на эмуляторе/устройстве.

---

## Вариант 3: Быстрый тест без покупок

Если нужно просто протестировать тесты без покупок:

1. Откройте `App.tsx`
2. Временно измените строку 47:
```typescript
// Было:
setIsPurchased(false);

// Стало:
setIsPurchased(true);  // Разрешить доступ без покупки
```

3. Запустите:
```bash
npm run web
```

---

## Пошаговая инструкция для Windows

### 1. Установите Node.js
- Скачайте с https://nodejs.org/
- Установите (выберите "Add to PATH")
- Проверьте: `node --version` в командной строке

### 2. Откройте Git Bash в папке проекта
```bash
cd E:/sait_printer/TestApp
```

### 3. Установите зависимости
```bash
npm install
```

Это займет 2-5 минут при первом запуске.

### 4. Запустите приложение

#### Для веб-версии:
```bash
npm run web
```

#### Для Android (если установлен Android Studio):
```bash
npm run android
```

---

## Решение проблем

### Ошибка: "npm не найден"
- Установите Node.js и перезапустите терминал

### Ошибка: "expo не найден"
- Выполните: `npm install -g expo-cli` или используйте `npx expo`

### Android эмулятор не запускается
- Установите Android Studio
- Создайте виртуальное устройство в AVD Manager
- Убедитесь, что включен USB Debugging на реальном устройстве

### Покупки не работают
- Покупки работают только в собранном приложении (не в Expo Go)
- Для тестирования используйте тестовые аккаунты Google Play
- См. инструкцию в `PLAY_STORE_SETUP.md`

---

## Команды для разработки

```bash
# Запуск веб-версии
npm run web

# Запуск на Android
npm run android

# Запуск на iOS (только на Mac)
npm run ios

# Запуск сервера разработки
npm start

# Сборка для production
npx eas build --platform android --profile production
```

---

## Рекомендации

1. **Для быстрого тестирования** → используйте `npm run web`
2. **Для тестирования на телефоне** → используйте Expo Go
3. **Для тестирования покупок** → соберите production версию через EAS Build




