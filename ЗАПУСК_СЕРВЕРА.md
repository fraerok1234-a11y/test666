# 🚀 Как запустить сервер разработки

## Проблема: порт 19006 не отвечает

Если `http://localhost:19006` не работает, выполните следующие шаги:

## Решение 1: Запуск с другим портом

1. Откройте командную строку или Git Bash
2. Перейдите в папку проекта:
   ```bash
   cd E:\sait_printer\TestApp
   ```
3. Запустите сервер:
   ```bash
   npx expo start --web --port 8083
   ```
4. Откройте браузер по адресу: `http://localhost:8083`

## Решение 2: Освободить порт 8081

Если порт 8081 занят другим процессом:

1. Найдите процесс, использующий порт:
   ```bash
   netstat -ano | findstr :8081
   ```
2. Завершите процесс (замените PID на номер процесса):
   ```bash
   taskkill /PID <номер_процесса> /F
   ```
3. Запустите сервер:
   ```bash
   npm run web
   ```

## Решение 3: Использовать статический файл

Если Expo не запускается, можно открыть статическую версию:

1. Откройте файл `TestApp/index.html` напрямую в браузере
2. Или используйте простой HTTP сервер:
   ```bash
   cd TestApp
   npx http-server -p 8080
   ```
3. Откройте `http://localhost:8080`

## Быстрый запуск (Windows)

Дважды кликните на файл `start.bat` в папке TestApp

## Проверка

После запуска вы должны увидеть:
- Сообщение "Starting Metro bundler"
- Адрес типа `http://localhost:8083`
- В браузере откроется приложение с тестами

## Если ничего не помогает

1. Убедитесь, что Node.js установлен:
   ```bash
   node --version
   ```
2. Переустановите зависимости:
   ```bash
   cd TestApp
   npm install
   ```
3. Очистите кеш:
   ```bash
   npx expo start --clear
   ```



